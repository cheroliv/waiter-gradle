= Cadrage Méthode 6 — Backlog — waiter-plugin
@CherOliv
2026-05-07
:jbake-title: Cadrage Méthode 6 — Backlog
:summary: Backlog priorise EPIC par EPIC.

== EPIC-0 — Onboarding

|===
| ID | User Story | Criteres d'acceptation | Estimation
| US-0.1 | En tant que dev, je veux `git clone engine && ./gradlew tasks` pour voir les taches disponibles. | `git clone` → `./gradlew tasks` liste les taches N0/N1/N2. | 3
| US-0.2 | En tant que dev, je veux `docker compose up` avec engine monte en volume. | `docker compose up -d` → engine accessible → `./gradlew tasks` fonctionnel. | 3
| US-0.3 | En tant que dev, je veux un README decrivant prerequis et methodes d'installation. | README.adoc avec Docker + JDK local + usage rapide. | 1
| US-0.4 | En tant que dev, je veux un Dockerfile buildable dans engine/. | `docker build -t cheroliv/engine .` → build OK. | 2
|===

== EPIC-1 — Execution

|===
| ID | User Story | Criteres d'acceptation | Estimation
| US-1.1 | `./gradlew aggregateGraphs` produit le diagramme du DAG. | Shell out → Gradle Daemon → stdout + fichier produit. | 5
| US-1.2 | `./gradlew decomposeIntention -Pintention="..."` produit un plan. | Parametre passe → planner execute → plan JSON/stdout retourne. | 5
| US-1.3 | `./gradlew {tache} --mode=ci` deporte sur CI/CD. | Commit+push → workflow declenche → `git pull` recupere le resultat. | 8
| US-1.4 | Message d'erreur lisible si tache echoue. | Exit code != 0 → stderr capture → message formate. | 2
| US-1.5 | Timeout configurable par tache. | `engine { taskTimeout.set(600) }` → tache tuee apres timeout → erreur. | 2
|===

== EPIC-2 — RAG

|===
| ID | User Story | Criteres d'acceptation | Estimation
| US-2.1 | `./gradlew indexCodebase` indexe le workspace dans pgvector. | Indexation → confirmation "N documents indexes". | 5
| US-2.2 | `./gradlew queryRag -Pquestion="..."` interroge le RAG. | Question passee → reponse contextuelle retournee. | 8
| US-2.3 | Codex alimente le RAG en amont. | `./gradlew extractBook → deriveOntology → exportKnowledgeBase` pipeline. | 13
|===

== EPIC-3 — Publication

|===
| ID | User Story | Criteres d'acceptation | Estimation
| US-3.1 | `./gradlew publishSite` publie un site. | Bakery declenche → push Git → site en ligne → URL retournee. | 3
| US-3.2 | `./gradlew serveDeck` previsualise un deck. | Slider serve → URL locale → preview navigateur. | 2
|===

== EPIC-4 — Qualite

|===
| ID | User Story | Criteres d'acceptation | Estimation
| US-4.1 | Tests TestKit pour le plugin. | `./gradlew test` → PluginFunctionalTest + ShellTaskTest → all green. | 5
| US-4.2 | CI GitHub Actions sur chaque PR. | `.github/workflows/build.yml` → `./gradlew build` → tests + lint. | 2
| US-4.3 | Publication Gradle Plugin Portal. | `./gradlew publishPlugins` → plugin sur plugins.gradle.org. | 2
|===
